import { describe, it } from 'vitest'

// Keyboard navigation tests disabled for now — they will be re‑implemented using Vitest/Browser.
describe('Keyboard Navigation', () => {
  it('placeholder test', () => {
    // No actual test until we migrate to Vitest/Browser.
  })
})
