- Create referral link does nothing
- No skills found despite having local skills (global) in @ picker
- File entries in the file picker should fade in spring
- The file picker still does not use the activeWorkspaceId as a root, should not default to "." but hint the user if there is no active workspace
- Open folder of skill does nothing
- Sync registry button has a flash ungraceful when clicked
- When i send a message nothing happen no conversation added in the left panel and no message in the center panel
