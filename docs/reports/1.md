- Global lint rules disabling has no effect on linting of local skills (as well as registry skills i guess) the config might not be passed and it use the default one
- Should have ui options in the settings especially to set max height code blocks
- learn more about linting should redirect to doc once url is live
- Still see the skill path in each lint warning panel card
- When i click on bookmark in the assistant message more dropdown, and reclick on the dropdown i should see the bookmark icon filled with the brand color, and the dropdown item should show unbookmark instead of bookmark
- When there is not many messages in the convresation, the hover card of the thread navigator appear with a too big height relative to it's content, should have max-height but dynamic one if not many messages
- Headings of assistant messages should have a bookmark icon appear on hover, and this should be reflected in the thread navigator
