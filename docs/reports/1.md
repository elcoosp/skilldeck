- Global lint rules disabling has no effect on linting of local skills (as well as registry skills i guess) the config might not be passed and it use the default one
- learn more about linting should redirect to doc once url is live: shell.open not allowed. Permissions associated with this command: shell:allow-open, shell:default
- horizontal scrollbar that should not happen on codeblock is not using the thin-scrollbar style from App.css
- Token usage input output is not computed should use precise solutions
- Bookmarks do not survive restart in the ui despite persistence, should only rely on db
