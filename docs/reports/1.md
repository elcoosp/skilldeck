- Cancel button in message input has no effect, no reflection in the ui the user message was canceled should have retry button
- message input scrollbar thumb is gray should be same as thin scrollbar
- Message input copy long text then select all and delete does not readjust input height
