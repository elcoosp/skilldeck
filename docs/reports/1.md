- Cancel button in message input has no effect, and no reflection in the ui the user message was canceled should have retry button
- message input scrollbar is not visible should be thin-scrollbar
- Message input copy long text then select all and delete does not readjust input height, but line by line delete does
- hover over cross icon in attachement item (file) is not visible black on black
- Analytics should show a tabbed heatmap of messages/conversations per day
- Markdown codeblock header instead of showing code should show the actual language of the codeblock or if found near the codeblock (or inside the codeblock first line comment look like path ?) the filename copyable on click
- When drag and dropping to a different conversation that the active one, the attachement file is still shown in the active conversation input, the root cause issue is deeper, we should have a draft persisted in the db with input content and context items per conversation (persisted on debounced change or conversation switch) so that we show the correct draft in the message-input for the correct conversation
- When dropping an attachment to another conversation than the active one we should focus the target conversation
- Should warn attachment not uploaded (drag &  drop) if attaching already attached content
- Double toast notification when drag & dropping in the global-drop-zone
