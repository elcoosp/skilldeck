- Highlight is not always applied on search because snippet use case insensitive and ui case sensitive
- Case sensitivity should be customizable in the search input and should allow regex search
- More icon on assistant message should have a bookmark option, with an option in the thread navigator to display only bookmarked messages via filter, and make it visual that it's bookmarked, with unbookmark on icon click, we should also be able to bookmark intra message content like headings. With full tauri commands integration
- Download message in assistant message more options does nothing real
- cancelAgent does only stop partially the streaming, but once the message start really straming and en i still got a new message
- Initial loading of a long list is not good performance wise, the ui block while messages are not yet rendered yet
- CLick on assistant message more button is very long, lag
