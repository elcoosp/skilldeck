-Drag and drop files or folders (supported formats) to attach context items to the conversation, zone should be whole window with blurred bg
- Conversation folder ui in left panel
- When i relint in skill detail panel a skill after changing lint global configuration, the lint warning still appear despite being disabled, disable this rule in individual card has no effect either
- Horizontal scrollbar that should not appear (text should be wrapped) on codeblock is not using the thin-scrollbar style from App.css, the vertical scrollbar is
- On restart jump to bottom in center-panel reshow the total of messages in the badge, it should not because message have already been seen, we should has a seen flag to the messsages table in the initial migration table and update the logic to have a consistent view across restart of the application
- Bookmarks do not survive restart in the ui despite persistence, should only rely on db
- Highlight gray background of dot items that have no chevron is longer than those with chevron should be same width
